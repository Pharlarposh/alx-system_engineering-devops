#!/usr/bin/env bash
# This script creates an RSA key pair in the current directory

# Create RSA key pair with 4096 bits and passphrase 'betty'
ssh-keygen -t rsa -b 4096 -f ./school -m PEM -C "Generated by script" -N 'betty'

echo "RSA key pair created successfully."

